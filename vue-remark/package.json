{
  "name": "vue-remark",
  "private": true,
  "scripts": {
    "build": "gridsome build",
    "develop": "gridsome develop",
    "explore": "gridsome explore",
    "deploy": "npm run build; rm -fR ../docs/*; cp -fR dist/* ../docs/; touch ../docs/nojekyll; git add ../docs; git ci -am deploy; git push"
  },
  "dependencies": {
    "@gridsome/vue-remark": "^0.2.6",
    "gridsome": "^0.7.0"
  }
}
